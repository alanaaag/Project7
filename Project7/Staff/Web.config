<configuration>
	<system.web>
		<authorization>
			<!-- Deny anonymous users and unauthenticated users by default -->
			<deny users="?"/>
			<allow users="*"/>
			<!-- Allow specific roles or additional settings for Staff -->
		</authorization>
	</system.web>
</configuration>